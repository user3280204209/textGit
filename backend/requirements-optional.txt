# ============================================================
# 可选依赖 —— 只有对应功能的负责人需要安装
# ============================================================
# 安装方式：
#   pip install -r requirements-optional.txt
# 或者只装你负责的那一组（推荐，省时间）：
#   pip install -r requirements-optional.txt  # 见下方分组按需选择
#
# ⚠️ 组员默认不要执行本文件。这些包体积大、平台差异大，
#    统一安装会显著拉长所有人的环境搭建时间并引入失败点。
# ============================================================

# ---- 组 A：文档与 Office 产出增强 ----
# 负责「专业课程讲解文档」「拓展阅读材料」的同学需要
# pdfplumber>=0.11,<1.0            # 解析上传的 PDF 教材
# python-pptx 已在主 requirements 中

# ---- 组 B：题库与自动判分 ----
# 负责「不同类型练习题目」的同学需要
# sympy>=1.13,<2.0                 # 数学题符号求解与答案校验

# ---- 组 C：多模态视频 / 动画生成 ----
# 负责「多模态教学视频/动画」的同学需要（三选一，不要全装）
#
# 方案 C1（推荐，纯远程 API，零本地依赖）：
#   不装任何包，直接调用文生视频 / 文生图 API
#
# 方案 C2（本地渲染动画，需要额外系统级 FFmpeg）：
# manim>=0.18,<1.0
#
# 方案 C3（HTML5 动画 + 无头浏览器录制）：
# playwright>=1.48,<2.0

# ---- 组 D：向量检索 / 语义召回 ----
# 负责「精准推送」的同学需要
# ⚠️ 注意：这些包在 Windows + Python 3.13 上可能没有预编译 wheel。
#    优先用「远程 Embedding API + SQLite/numpy 余弦相似度」，
#    确实需要本地向量库时再启用下面这行：
# chromadb>=0.5,<2.0

# ---- 组 E：数据库服务端（答辩部署用）----
# 本机开发用 SQLite 就够，只有部署到服务器时才需要
# asyncpg>=0.30,<1.0
# psycopg[binary]>=3.2,<4.0

# ---- 组 F：鉴权（若做多用户）----
# passlib[bcrypt]>=1.7,<2.0
# pyjwt>=2.9,<3.0
